"""Ponto de entrada da Huli 4 durante a Fase 0."""

from huli import __app_name__, __version__


def main() -> None:
    """Executa o ponto de entrada mínimo enquanto a fundação é construída."""
    print(f"{__app_name__} {__version__} — Fase 0 em construção.")


if __name__ == "__main__":
    main()
